-let overviews=[['Sean','龍哥肉燥飯',"7:30","10:30"],['dargon','test_shop',"9:30","11:30"]]
    each val,i in overviews
            .body_div 店名:  #{val[0]}
                    p 地址:#{val[1]}
                    p 結束時間:#{val[3]}
                

    script.
        for (let i in document.querySelectorAll(".body_div"))
            document.querySelectorAll(".body_div")[i].id = i;
        $(function(){
            $(".body_div").click(function(click_id){
                let c_id = this.id;
                console.log(c_id);
                    var url='/order';
                    $.ajax({
                        url:'/order',
                        type:'GET',
                        data:{
                        },
                        success:function(url){
                            $('.main_body').html(url);
                            }
                            });
                            return false;
                        });
                        });

